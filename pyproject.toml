[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "tests",
]
python_files = "test_*.py"

[tool.ruff]
# Update target to match project requirement
target-version = "py312"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "B", "I"]
ignore = []
